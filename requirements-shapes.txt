# Install with --no-deps --target .cache/pyrefly-shapes (Python 3.12+).
# Keep the shape extensions and PyTorch overlay in lockstep with Pyrefly.
pyrefly-torch-stubs==1.3.1
pyrefly-shape-extensions==1.3.1
jaxtyping==0.3.11
